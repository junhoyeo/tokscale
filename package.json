{
  "name": "tokscale-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Token Usage Leaderboard - The Kardashev Scale for AI Devs",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "node -e \"if(!process.env.VERCEL){require('child_process').execSync('bun run build:core',{stdio:'inherit'})}else{console.log('Skipping core build on Vercel')}\"",
    "build": "bun run build:core && bun run build:cli",
    "build:cli": "bun run --cwd packages/cli build",
    "build:core": "bun run --cwd packages/core build",
    "cli": "./scripts/cli.sh",
    "dev:frontend": "bun run --cwd packages/frontend dev",
    "dev:benchmarks": "bun run --cwd packages/benchmarks run"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.4.1",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
