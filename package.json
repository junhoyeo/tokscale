{
  "name": "token-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Token Usage Leaderboard - The Kardashev Scale for AI Devs",
  "type": "module",
  "workspaces": [
    "packages/*",
    "frontend",
    "benchmarks"
  ],
  "scripts": {
    "postinstall": "bun run build:core",
    "build": "bun run build:core && bun run build:cli",
    "build:cli": "bun --filter @0xinevitable/token-tracker run build",
    "build:core": "bun --filter @0xinevitable/token-tracker-core run build",
    "cli": "bun --filter @0xinevitable/token-tracker run dev",
    "dev:frontend": "bun --filter @token-tracker/frontend run dev"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.4.1",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
